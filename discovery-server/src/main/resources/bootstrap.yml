spring:
  application:
    name: discovery-service
  cloud:
    config:
      fail-fast: true
      name: discovery-service
      uri: http://localhost:8888
  profiles:
    active: local

# -Dspring.cloud.config.enabled=false (sa rulez fara config)